# CafePulse Offline Licensing Secrets Template
#
# INSTRUCTIONS:
# 1. Copy this file to `secrets.py` in the same directory:
#    cp secrets.py.template secrets.py
# 2. Change the SECRET_SALT value below to your production secret salt.
# 3. Keep `secrets.py` strictly private. It is already added to `.gitignore`.
# 4. If `secrets.py` is missing, the application will automatically fall back
#    to a safe development-only salt.

SECRET_SALT = "CafePulseDeveloperOfflineSecretSaltFallback!!!"
